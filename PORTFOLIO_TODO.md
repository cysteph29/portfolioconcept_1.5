# Portfolio — backlog & follow-ups

Open items for this site that aren’t tied to a single PR. Grouped by area so you can scan quickly.

---

## Work section — project cards

### Media (placeholder → final)

- Replace the flat `--color-sky` placeholder with the real treatment from design (pattern, image asset, aspect ratio or height rules, optional lazy loading).
- Add a prototype video or hero image for each project card (asset, aspect ratio, optional poster frame, lazy loading).
- Wire props or CMS fields when project data moves out of the component.

### Whole-card links — hover, focus, and press

- Define hover styling for the whole-card link (e.g. subtle lift, border, or background shift) in line with the rest of the site.
- Add explicit `focus-visible` styles so keyboard users get a clear ring or outline that respects `border-radius`.
- Consider `:active` / pressed state if motion or affordance is part of the design system.

---

## Work experience

- Update icons for the work experience section.

---

## Global / other (add here as needed)

_Use this section for navigation, hero, footnotes, CMS, analytics, etc., when you want everything in one place._

### Navigation

- Style nav elements (layout, type, spacing, active/current state, hover, and `focus-visible`) to match the rest of the site.
- Make the navigation sticky (position, backdrop or border, z-index, and scroll behavior so layout stays predictable).

### Homepage

- Add subtle text or section loading animation for all sections on the homepage.
